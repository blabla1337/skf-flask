export class ProjectStats {
  constructor(
  project_name: string,
  project_desc: string,
  project_level: number,
  project_id: number,
  project_open: number,
  project_closed: number,
  prpject_accepted: number
  ) {}
}
