import { LinksFilterPipe } from './linksfilter.pipe';

describe('LinksFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LinksFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
